// echo python code
pipeline {
    agent any
    stages {
        stage('Python') {
            steps {
                sh 'python --version'
                sh 'python test.py'
            }
        }
    }
}
```